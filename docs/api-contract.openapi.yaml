openapi: 3.0.3
info:
  title: Push Notification Platform API
  version: 1.0.0
  description: |
    Contract between the demo React client (or any external system, e.g. .NET) and the Node.js notification gateway.

    **Identifiers:** `playerId` is the OneSignal Web **subscription id** returned by the browser SDK (`OneSignal.User.PushSubscription.getIdAsync()` in SDK v16+). The backend treats it as an opaque string and persists it in PostgreSQL.

    **OneSignal alignment:** Targeting a named `segment` uses a OneSignal user tag `segment=<value>`. The demo client sets this tag when subscribing so `/send?segment=` can filter recipients via OneSignal `filters`.
servers:
  - url: http://localhost:3000
    description: Local development
paths:
  /health:
    get:
      summary: Service health
      responses:
        '200':
          description: API and database reachable
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
                  database:
                    type: string
                    example: connected
  /subscribe:
    post:
      summary: Register or refresh a push subscription
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [playerId]
              properties:
                playerId:
                  type: string
                segment:
                  type: string
                  default: general
                  description: Logical audience label mirrored as OneSignal tag `segment`
      responses:
        '200':
          description: Subscription stored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionRecord'
  /unsubscribe:
    post:
      summary: Mark subscription inactive in PostgreSQL
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [playerId]
              properties:
                playerId:
                  type: string
      responses:
        '200':
          description: Updated subscription
        '404':
          description: Unknown playerId
  /subscribers:
    get:
      summary: List all stored subscriptions (player id and segment)
      description: Returns every row in the subscriptions table, newest `updatedAt` first. Treat as sensitive data.
      security:
        - ApiKeyAuth: []
      responses:
        '200':
          description: List of subscribers
          content:
            application/json:
              schema:
                type: object
                required: [subscribers]
                properties:
                  subscribers:
                    type: array
                    items:
                      $ref: '#/components/schemas/SubscriberSummary'
        '401':
          description: Missing or invalid API key
  /status/{playerId}:
    get:
      summary: Read subscription flags from PostgreSQL
      parameters:
        - name: playerId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Current persisted status
          content:
            application/json:
              schema:
                type: object
                properties:
                  isSubscribed:
                    type: boolean
                  segment:
                    type: string
  /send:
    post:
      summary: Trigger a push via OneSignal (protected)
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title, message, url]
              properties:
                title:
                  type: string
                message:
                  type: string
                url:
                  type: string
                  format: uri
                  description: Opened when the notification is tapped
                segment:
                  type: string
                  nullable: true
                  description: If set, targets users tagged `segment` in OneSignal. If omitted, targets the built-in `Subscribed Users` segment.
      responses:
        '202':
          description: Accepted and logged as sent
        '502':
          description: Logged as failed (OneSignal error)
        '401':
          description: Missing or invalid API key
components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: x-api-key
  schemas:
    SubscriptionRecord:
      type: object
      properties:
        id:
          type: string
          format: uuid
        playerId:
          type: string
        isSubscribed:
          type: boolean
        segment:
          type: string
    SubscriberSummary:
      type: object
      properties:
        id:
          type: string
          format: uuid
        playerId:
          type: string
          description: OneSignal subscription id
        segment:
          type: string
        isSubscribed:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
